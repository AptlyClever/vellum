fastapi>=0.115.0
uvicorn>=0.32.0
pyyaml>=6.0.2
pydantic>=2.0
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27.0
pillow>=10.0
# OAuth 2.0 for the fleet (backend/auth/). `httpx` above is also the
# client-credentials transport. The [crypto] extra is required, not optional: without
# `cryptography` PyJWT cannot verify the RS256 signatures Keycloak issues, and
# backend/auth/jwt_validator.py imports PyJWKClient unconditionally at module load.
pyjwt[crypto]>=2.8.0
# Fleet-wide Postgres (PX001). backend/intake.py's IntakeRun ledger is the
# first Postgres-backed store in Vellum -- see
# deploy/postgres/migrations/vellum/001__intake_runs.sql for what it replaced.
psycopg[binary]>=3.2.0
