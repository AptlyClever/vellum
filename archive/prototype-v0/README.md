# prototype-v0 archive

Frozen 2026-07-14 when Vellum pivoted from Capture science-project to the Asset Pipeline product (`docs/asset-pipeline-product.md`).

This folder holds:

- one-off Aurora diag / kick / cutover scripts (`tmp-scripts/`, `tools-unreal-scratch/`)
- duplicate install/host copies that fought `config/` and `tools/unreal/host-install/`
- misc verify scripts

Do not resume these as SoT. Useful patterns were salvaged into `tools/pipeline/`.
