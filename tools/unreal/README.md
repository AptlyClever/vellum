# tools/unreal — host helpers (historical + remaining)

**Product control plane:** [`docs/asset-pipeline-product.md`](../../docs/asset-pipeline-product.md) and [`tools/pipeline/`](../pipeline/).

This folder retains UE host profiles, Library helpers, and **retired** Capture/Lookdev Worker scripts (`vellum_ue_agent.ps1`, `vellum_ue_worker.ps1`) gated behind unpark env vars.

Use `config/ue-hosts.json` (not a copy under this folder).
